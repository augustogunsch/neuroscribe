<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The dark the web app opens on, so the window behind the WebView is not
         a white flash on every cold start. -->
    <style name="Theme.Neuroscribe" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/background</item>
        <item name="android:statusBarColor">@color/background</item>
        <item name="android:navigationBarColor">@color/background</item>
    </style>
</resources>
