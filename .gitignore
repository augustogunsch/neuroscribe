/neuroscribe
neuroscribe.db
neuroscribe.db-*
.playwright-mcp/
# every local env file (secrets live here) except the checked-in template
.env
.env.*
!.env.example
backups/
# Both may be a directory or, while developing, a symlink to one fetched
# elsewhere; the trailing slash would only match the directory.
/pyodide
/typst
dist/
downloads/
